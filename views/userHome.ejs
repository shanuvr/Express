<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>User Home - Minimal</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">

  <div class="w-full max-w-md bg-white rounded-2xl shadow-xl p-8 flex flex-col items-center">
    <svg class="w-12 h-12 text-blue-500 mb-2" fill="none" viewBox="0 0 64 64">
      <circle cx="32" cy="32" r="28" stroke="currentColor" stroke-width="4" />
      <path d="M24 32l8 8 8-8" stroke="currentColor" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    <h2 class="text-2xl font-bold text-gray-800 mb-1">Hello, <%= user.name %>!</h2>
    <p class="text-gray-400 text-sm mb-8">Welcome to your dashboard</p>
    <div class="w-full space-y-4 mb-8">
      <div class="flex justify-between items-center">
        <span class="text-gray-500 font-semibold">Name:</span>
        <span class="text-gray-800 font-medium"><%= user.name %></span>
      </div>
      <div class="flex justify-between items-center">
        <span class="text-gray-500 font-semibold">Email:</span>
        <span class="text-gray-800 font-medium"><%= user.email %></span>
      </div>
      <div class="flex justify-between items-center">
        <span class="text-gray-500 font-semibold">Phone:</span>

        <span class="text-gray-800 font-medium"><%= user.phone %></span>

      </div>
    </div>
    <button class="w-full py-2 rounded-lg bg-blue-500 text-white font-semibold hover:bg-blue-600 transition" onclick="logoutuser()">
      Logout
    </button>
  </div>

  <script>
    function logoutuser(){
      fetch('/logout').then(()=>{
        window.location.href = '/'
      }).catch((err)=>{console.log(err);})
    }
  </script>
</body>
</html>
