<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Minimalist Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100">
 
  
<form class="w-full max-w-sm bg-white rounded-2xl shadow-xl p-8 space-y-6" action="/login" method="POST">
   <% if (message && message.length > 0) { %>
  <div class="max-w-md mx-auto my-4 px-4 py-2 bg-red-100 text-red-700 rounded shadow">
    <%= message[0] %>
  </div>
<% } %>
    <div class="flex flex-col items-center">
      <svg class="w-12 h-12 text-blue-500 mb-2" fill="none" viewBox="0 0 64 64">
        <circle cx="32" cy="32" r="28" stroke="currentColor" stroke-width="4" />
        <path d="M24 32l8 8 8-8" stroke="currentColor" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <h2 class="text-2xl font-bold text-gray-800 mb-1">Welcome Back</h2>
      <p class="text-gray-400 text-sm">Login to your account</p>
    </div>
    <div>
      <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
      <input id="email" name="email" type="email" required
        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 transition" 
        placeholder="you@example.com" />
    </div>
    <div>
      <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
      <input id="password" name="password" type="password" required
        class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 transition" 
        placeholder="••••••••" />
    </div>
    <button type="submit"
      class="w-full py-2 rounded-lg bg-blue-500 text-white font-semibold hover:bg-blue-600 transition">Login</button>
    <div class="flex justify-between text-xs text-gray-400">
      <a href="#" class="hover:text-blue-500 transition">Forgot Password?</a>
      <a href="#" class="hover:text-blue-500 transition">Create Account</a>
    </div>
  </form>
</body>
</html>
